{
  "name": "approov-worker",
  "version": "0.0.0",
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^2.1.3"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "wrangler": "^2.20.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --port 8787",
    "deploy": "wrangler publish",
    "test": "jest"
  }
}
