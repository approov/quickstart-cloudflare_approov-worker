{
  "name": "approov-worker",
  "version": "0.0.0",
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^2.1.3"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "wrangler": "^2.4.4"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --port 8787",
    "deploy": "wrangler publish",
    "test": "jest"
  }
}
