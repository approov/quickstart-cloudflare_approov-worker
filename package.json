{
  "name": "approov-worker",
  "version": "0.0.0",
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^2.1.2"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "wrangler": "^2.2.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "jest"
  }
}
