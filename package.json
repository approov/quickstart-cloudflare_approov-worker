{
  "name": "approov-worker",
  "version": "0.0.0",
  "dependencies": {
    "@tsndr/cloudflare-worker-jwt": "^3.1.4"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "wrangler": "^4.6.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --port 8787",
    "deploy": "wrangler publish",
    "test": "jest"
  }
}
